#!/usr/bin/env bash

# needs python >= 3.8
virtualenv venv -p python3
source venv/bin/activate

python3 -m pip install .[plaster]
plaster --version
psqlgml --version
plaster generate -p gdcdictionary -o src/gdcdatamodel2/models

rm -rf venv
