#!/usr/bin/env bash

# remove ~/.gml if plaster failed to get new dictionary.
# rm -rf ~/.gml

VENV=venv_plaster
# needs python >= 3.8
virtualenv $VENV -p python3.8
source $VENV/bin/activate

python3 -m pip install \
    --index-url https://nexus.osdc.io/repository/pypi-all/simple \
    plaster \
    "psqlgml>=0.2.4" \
    pre-commit \
    isort==5.12.0 \  # match version of pre-commit-config
    black==23.10.1   # match version of pre-commit-config
plaster --version
psqlgml --version
plaster generate -p gdcdictionary -o src/gdcdatamodel2/models -s src/gdcdatamodel2/partial_dictionary
isort src
black src
pre-commit run -a

rm -rf $VENV
